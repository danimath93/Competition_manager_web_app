# Dockerfile per sviluppo backend
FROM node:18-alpine

# Crea la directory dell'app
WORKDIR /app

# Copia i file package.json e package-lock.json
COPY package*.json ./

# Installa tutte le dipendenze (incluse quelle di sviluppo)
RUN npm install

# Installa nodemon globalmente per il hot-reload
RUN npm install -g nodemon

# Espone la porta 3050
EXPOSE 3050

# Comando per avviare l'applicazione in modalità sviluppo
CMD ["npm", "run", "dev"]
